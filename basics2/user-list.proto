syntax = "proto3";

import "user.proto";

message UserList {
    message Test {
        enum TestEnum {
            TEST_ENUM_1 = 0;
            TEST_ENUM_2 = 1;
        }
        TestEnum test_enum = 1;
    }
    User user = 1;
}
